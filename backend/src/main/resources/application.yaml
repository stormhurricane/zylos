spring:
  application:
    name: backend
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

logging:
  file:
    name: logs/backend.log
  level:
    com.zylos.backend: DEBUG
